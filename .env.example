# Database
POSTGRES_PASSWORD=devpass
# POSTGRES_PORT=5432

# Gateway
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin
RUST_LOG=info
# GATEWAY_PORT=8080
# CCAG_VERSION=latest       # Pin to a specific version: CCAG_VERSION=1.0.0

# AWS (required — gateway needs Bedrock access)
AWS_REGION=us-east-1
# AWS_PROFILE=default

# OIDC (optional — configure later via portal)
# OIDC_ISSUER=https://your-idp.example.com
# OIDC_AUDIENCE=your-audience
